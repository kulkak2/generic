#include <stdio.h>

int main() {
    int n = 12;
    int count = 0;
    while (n) {
        if (1&n) {
            count ++;
        }
        n = n>>1;
    }
    printf("\nNumber of 1s: %d", count);
    return 0;
}
